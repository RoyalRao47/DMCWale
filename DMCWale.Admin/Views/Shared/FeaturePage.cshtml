@model FeaturePageViewModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="row">
    <div class="col-sm-12">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <div class="panel-title">@Model.Title</div>
            </div>
            <div class="panel-body">
                <p class="text-muted">@Model.Module page is ready for implementation.</p>
            </div>
        </div>
    </div>
</div>
